# Endpoint that receives contact form submissions as JSON
# {name, email, message, source}. Without it the form shows
# a direct email fallback.
CONTACT_WEBHOOK_URL=

# Optional shared secret, sent as "Authorization: Bearer <value>"
# so the endpoint can reject calls that don't come from the site.
PORTFOLIO_CONTACT_SECRET=
